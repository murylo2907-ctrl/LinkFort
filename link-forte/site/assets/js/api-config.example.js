// Copie para api-config.js (gerado automaticamente no deploy via prepare-deploy.mjs)
// Dev local: site em http://localhost:3000 | API Next.js em http://localhost:3001
// LF_MOCK_API: true = checkout/pagamento mock no front (desligue quando a API estiver pronta)
window.LF_MOCK_API = true;
window.LF_API_BASE_URL = "http://localhost:3001";
